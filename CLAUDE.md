@AGENTS.md

<!-- O `@` acima e o de baixo são IMPORTS do Claude Code: o conteúdo dos dois
     arquivos entra no contexto no início da sessão. O de baixo é o que garante
     que o protocolo seja lido de fato, e não apenas linkado — um link depende
     de o agente resolver abri-lo. Custa contexto por sessão, e é barato perto
     do que custa uma frente fechada sem baseline. -->

@docs/AI-ENGINEERING-PROTOCOL.md
